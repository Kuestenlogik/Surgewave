<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <Description>Enterprise clustering features for Surgewave - Raft consensus, replication, and cluster coordination</Description>
  </PropertyGroup>

  <ItemGroup>
    <InternalsVisibleTo Include="Kuestenlogik.Surgewave.Clustering.Tests" />
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="Microsoft.Extensions.Logging.Abstractions" />
  </ItemGroup>

  <ItemGroup>
    <ProjectReference Include="..\Kuestenlogik.Surgewave.Core\Kuestenlogik.Surgewave.Core.csproj" />
    <!-- #59 b5 ATOMIC FLIP: Protocol.Kafka reference REMOVED. Clustering is now wire-neutral;
         the Kafka codecs (ControllerClient/BrokerLifecycleManager) moved into Protocol.Kafka,
         which now references Clustering (edge inverted). -->
    <ProjectReference Include="..\Kuestenlogik.Surgewave.Transport\Kuestenlogik.Surgewave.Transport.csproj" />
    <!-- #60 Inc2: the native inter-broker payloads reuse the SRWV reader/writer, opcodes and
         ISerializablePayload contract from Protocol.Native. That assembly is a leaf (no edge back to
         Clustering/Core), so this is a safe downward reference with no cycle. -->
    <ProjectReference Include="..\Kuestenlogik.Surgewave.Protocol.Native\Kuestenlogik.Surgewave.Protocol.Native.csproj" />
  </ItemGroup>

</Project>
