#!/bin/bash
set -e

if [ "$1" = "purge" ]; then
    # Remove data and config on purge
    rm -rf /var/lib/storm
    rm -rf /var/log/storm
    rm -rf /etc/storm

    # Remove user
    userdel storm 2>/dev/null || true
    groupdel storm 2>/dev/null || true
fi

systemctl daemon-reload
