apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: surgewave-system

resources:
  - crd.yaml
  - rbac.yaml
  - deployment.yaml

commonLabels:
  app.kubernetes.io/managed-by: surgewave-operator
